services:
  traefik:
    image: traefik:v3.0
    container_name: footapp_traefik
    restart: always
    security_opt:
      - no-new-privileges:true
    networks:
      - traefik_public
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOCKER_API_VERSION=1.44 # Critical fix for Ubuntu 24.04
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik_data/acme.json:/acme.json
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=traefik_public"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.myresolver.acme.tlschallenge=true"
      - "--certificatesresolvers.myresolver.acme.email=anibasoufiane2001@gmail.com" # Updated from your request
      - "--certificatesresolvers.myresolver.acme.storage=/acme.json"

networks:
  traefik_public:
    name: traefik_public # Explicit name so other composes can find it
